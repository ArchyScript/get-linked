// export default function ({ store, redirect }) {
//   if (store.getters.authUser) {
//     const { isKYC } = store.getters.authUser.profile;

//     if (!isKYC) {
//       redirect('/profile/kyc');
//       return;
//     }
//   }
// }
